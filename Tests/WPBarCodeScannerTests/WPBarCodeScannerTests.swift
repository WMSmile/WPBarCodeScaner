import XCTest
@testable import WPBarCodeScanner

final class WPBarCodeScannerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WPBarCodeScanner().text, "Hello, World!")
    }
}
